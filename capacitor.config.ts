import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.tpavan.lakshya',
  appName: 'Lakshya AP SI Prep',
  webDir: 'dist',
  android: {
    allowMixedContent: false,
    backgroundColor: '#142b38',
  },
}

export default config
